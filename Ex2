package com.Tutor;

import java.util.HashSet;
import java.util.Set;

public class Main {

    public static void main(String[] args) {
        int [] array = {4, 5, 6, 6, 8};

        Set<Integer> hs = new HashSet<>();

        Main main = new Main();
        System.out.println(main.solution(array, hs));
    }

    public boolean solution(int [] array, Set<Integer> set) {
        for (int i = 0; i < array.length; i++) {
            if (!set.add(array[i])) {
                return true;
            }
        }
        return false;
    }
}
